<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>User Details</h1>
    <h2><%= user.username %></h2>
    <h3>Logbook</h3>
    <ul>
        <% user.logbook.forEach((item) => { %>
         <li><%= item.exercise %></li>
        <% }) %>
    </ul>
    <a href="/socials">Back to Community</a>
</body>
</html>