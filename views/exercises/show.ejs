<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/show.css">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <title>Exercise Details</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= exercise.exercise %></h1>
    <h3>Exercise History</h3>

    <% if (exerciseHistory && exerciseHistory.length > 0) { %>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Sets</th>
                        <th>Reps</th>
                        <th>Weight</th>
                        <th>Notes</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% exerciseHistory.forEach((historyItem) => { %>
                        <tr>
                            <td><%= historyItem.sets %></td>
                            <td><%= historyItem.reps %></td>
                            <td><%= historyItem.weight ? historyItem.weight + ' kg' : '' %></td>
                            <td><%= historyItem.notes %></td>
                            <td><a href="/exercises/<%= exercise._id %>/<%= historyItem._id %>/edit">Edit or Delete entry</a></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    <% } else { %>
        <p>No history found for this exercise.</p>
    <% } %>

</body>
</html>
