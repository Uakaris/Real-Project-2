<!-- first draft of a new index page -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logbook</title>
</head>
<body>
    <% include('../partials/_navbar.ejs') %>
    <h1>Logbook</h1>
    <a href="/exercises/new">Log a new exercise</a>
    <ul>
        <% 
        // Object to store the most recent exercise entry for each exercise type
        const latestEntries = {};

        exercises.forEach((exercise) => {
            // Check if this exercise type is already in latestEntries
            if (!latestEntries[exercise.exercise] || exercise.createdAt > latestEntries[exercise.exercise].createdAt) {
                latestEntries[exercise.exercise] = exercise;
            }
        });

        // Iterate through latestEntries to display the latest exercise for each type
        Object.values(latestEntries).forEach((exercise) => { %>
            <li>
                <h2><a href="/exercises/<%= exercise._id %>"><%= exercise.exercise %></a></h2>
                <p>Sets: <%= exercise.sets %></p>
                <p>Reps: <%= exercise.reps %></p>
                <% if (exercise.weight) { %>
                    <p><%= exercise.weight %> kg</p> 
                <% } %> 
                <% if (exercise.notes) { %>
                    <p>Notes: <%= exercise.notes %></p>
                <% } %>
                <a href="/exercises/<%= exercise._id %>/edit">Edit or Delete entry</a>
            </li>
        <% }) %>
    </ul>
</body>
</html>

// original index page
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logbook</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Logbook</h1>
    <a href="/exercises/new">Log a new exercise</a>
        <ul>
            <% exercises.forEach((exercise) => { %>
             <li>
                <h2><a href="/exercises/<%= exercise._id %>"><%= exercise.exercise %></a></h2>
                <!-- <p>Sets: <%= exercise.sets %></p>
                <p>Reps: <%= exercise.reps %></p>
                <% if (exercise.weight) { %>
                    <p><%= exercise.weight %>kg</p> 
                <% } %> 
                <% if (exercise.notes) { %>
                    <p>Notes: <%= exercise.notes %></p>
                <% } %> -->
                <a href="/exercises/<%= exercise._id %>/edit">Edit or Delete entry</a>
             </li>
            <% }) %>
        </ul>
</body>
</html>

// original user/show.ejs page

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %>'s Logbook</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= user.username %>'s Logbook</h1>
    <% if (user.exercises && user.exercises.length > 0) { %>
        <ul>
            <% user.exercises.forEach((exercise) => { %>
                <li>
                    <h2><%= exercise.exercise %></h2>
                    <p>Sets: <%= exercise.sets %></p>
                    <p>Reps: <%= exercise.reps %></p>
                    <% if (exercise.weight) { %>
                        <p>Weight: <%= exercise.weight %> kg</p>
                    <% } %>
                    <% if (exercise.notes) { %>
                        <p>Notes: <%= exercise.notes %></p>
                    <% } %>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No exercises found.</p>
    <% } %>
    <a href="/socials">Back to Community</a>
</body>
</html>

// old bav bar

<nav>
    <a href="/">Home</a>
    <% if (user) { %>
     <a href="/exercises">Logbook</a>
     <a href="/socials">Community</a>
     <a href="/authentication/sign-out">Sign Out</a>
    <% } else { %>
        <a href="/authentication/sign-in">Sign In</a>
        <a href="/authentication/sign-up">Sign Up</a>
    <% } %>
</nav>

// index page as at 27/06 @ 10:50am

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vault</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <p><a href="/exercises/new">Log a new exercise</a></p>
    <ul>
        <% 
        const latestEntries = {};
        
        exercises.forEach((exercise) => {
            if (!latestEntries[exercise.exercise] || exercise.createdAt > latestEntries[exercise.exercise].createdAt) {
                latestEntries[exercise.exercise] = exercise;
            }
        });

        Object.values(latestEntries).forEach((exercise) => { %>
            <li>
                <h2><a href="/exercises/<%= exercise._id %>"><%= exercise.exercise %></a></h2>
            </li>
        <% }) %>
    </ul>
</body>
</html>

// edit.ejs code as at 27/06 at 4:30pm
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/edit.css">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <title><%= title %>: Edit Entry</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="container">
            <h1>Edit Entry</h1>
                <form action="/exercises/<%= exercise._id %>/<%= historyItem._id %>?_method=PUT" method="post">
                    <label for="exercise" class="form-label">Exercise</label>
                        <input list="exercises" name="exercise" class="form-control" value="<%= exercise.exercise %>"/>
                    <label class="form-label" >
                        Sets:
                            <input type="number" name="sets" class="form-control" value="<%= historyItem.sets %>" required>
                    </label>
                    <label class="form-label" >
                        Reps:
                            <input type="number" name="reps" class="form-control"  min="1" value="<%= historyItem.reps %>" required>
                    </label>
                    <label class="form-label" >
                        Weight:
                            <input type="number" name="weight" class="form-control" min="0" value="<%= historyItem.weight %>">
                    </label>
                    <label class="form-label">
                        Notes:
                        <textarea name="notes" class="form-control" ><%= historyItem.notes %></textarea>
                    </label>
                        <button class="btn btn-secondary">Add edited exercise</button>
                </form>
                    <form class="delete" action="/exercises/<%= exercise._id %>/<%= historyItem._id %>?_method=DELETE" method="post">
                        <button class="btn btn-secondary" >Delete Entry</button>
                    </form>

    </div>
</body>
</html>